# 工作日誌 / Work Log

節奏繞纏 / Rhythmic Entanglements — ITRI 藝術進駐專案。
記錄每次工作的重點與決策（最新在最上面）。

---

## 2026-07-07 — 加入 Ableton Live demo 專案 + Git LFS 設定

**環境：** Mac mini

**做了什麼**
- 把桌面上的 Ableton 專案「同動車 Live Demo」納入 repo，放在 `live_demo/`。
  - 專案 + 檔名改成純英文：`Live_Demo_Project/Live_Demo.als`（避免中文/空白造成路徑問題）。
- **設定 Git LFS**：`.gitattributes` 讓 `live_demo/` 底下的音檔（wav/aif/aiff/mp3/flac/m4a）走 LFS，未來 Collect All and Save 的 sample 不會撐爆 git 歷史。
  - 既有的 `paired_motion/media/` 音檔維持原樣（純 git，未受影響）。
  - 需先 `brew install git-lfs && git lfs install`。
- **`.gitignore`** 新增：Ableton `Backup/` 自動備份、`Icon` 檔不進 repo。
- 桌面捷徑：一開始用 `ln -s` symlink，會每次跳「Verifying…」且開不了 → 改成 **macOS 原生 Finder alias**（並清掉 `.als` 的 quarantine 屬性）後正常。
- 手動把桌面原本的舊資料夾移走，避免與 repo 分身混淆。
- 也更新了 `paired_motion` 的 `main.maxpat` / `sub_tof.maxpat`（tof mapping 與接線微調）。

**決策 / 慣例**
- Ableton 專案未來音檔一律走 Git LFS，只針對 `live_demo/`。
- 以後只動 repo 裡的檔案，不另存分身。
- 換電腦流程與細節見 `live_demo/README.md`。

---
