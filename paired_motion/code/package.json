{
  "name": "paired-motion",
  "version": "1.0.0",
  "description": "Paired Motion 演出 patch — 同動車三組感測器整合（auto-detect via WHO handshake）",
  "main": "auto_detect.js",
  "private": true,
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "serialport": "^12.0.0"
  }
}
