# OS
.DS_Store

# Python
__pycache__/
*.pyc
venv/
.venv/

# Node
node_modules/

# IDE
.vscode/
.idea/

# Max/MSP backup
*.maxpat.bak

# Ableton auto-backups (git already versions the .als)
Backup/
Icon
Icon?

# Ableton sample analysis cache — Live rewrites these on every open, so
# tracking them means guaranteed binary conflicts between machines.
# Warp data lives in the .als (WarpMarkers/WarpMode per SampleRef), not here;
# Live re-analyses missing .asd automatically on first load.
*.asd

# Claude Code
.claude/
